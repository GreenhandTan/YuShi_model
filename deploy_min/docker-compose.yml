version: '3.8'

services:
  audit:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./checkpoints:/app/checkpoints:ro
    restart: unless-stopped
